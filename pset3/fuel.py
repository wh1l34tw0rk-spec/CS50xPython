def check_fuel(amount):
    # Split string at "/" + Convert to int
    # Convert fraction to input
    #
    #
    return amount


def main():
    print("Can't read fractions? Enter your fuel value and get back a percentage")
    fuel_level = input("Fraction: ")
    fuel = check_fuel(fuel_level)
    print(fuel)

if __name__ == "__main__":
    main()

